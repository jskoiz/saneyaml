case = "empty-block-scalar-event-shape"
policy = "Empty block scalar tree values are shared while parser-event spelling can diverge"
prototype = "Accepts YAML-suite K858 and loads the empty block scalar as an empty string, matching the shared tree/value acceptance surface."
serde_yaml = "serde_yaml 0.9.34 accepts YAML-suite K858 as an empty block scalar value."
yaml_rust2 = "yaml-rust2 0.11.0 accepts K858 but its parser-event stream reports the empty folded block scalar as null-like text rather than the prototype's empty string event value."
saphyr = "saphyr 0.0.6 accepts K858 but its parser-event stream reports the empty folded block scalar as null-like text rather than the prototype's empty string event value."
decision = "Keep K858 in tree and shared-reference acceptance, and keep it out of normalized event parity until event-level empty block scalar spelling is deliberately normalized."
