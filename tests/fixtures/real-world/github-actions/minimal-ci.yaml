name: CI
on:
  push:
    branches: [main]
  pull_request: null
env:
  CARGO_TERM_COLOR: always
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: cargo test --all
        with:
          profile: ci
