apiVersion: v1
kind: ConfigMap
metadata:
  name: yaml-demo-config
data:
  app.yaml: |-
    # this comment is data
    server:
      port: 8080

      # blank line above is data
  message.txt: |
    hello

    world
